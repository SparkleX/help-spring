package help;

import org.junit.jupiter.api.Test;
import org.mockito.MockedStatic;
import org.mockito.Mockito;
import org.springframework.boot.SpringApplication;

public class FooApplicationTest {
    @Test
    void function1() {
    	//String[] args = new String[5];
    	//MockedStatic<SpringApplication> mockSpringApp = Mockito.mockStatic(SpringApplication.class);
    	//mockSpringApp.when(
    	//		() -> SpringApplication.run(FooApplication.class, args)).thenReturn(null);
	    
    	
    	//FooApplication.main(args);
    }
}
